%   ┌───────────────────────────────────────────────────────┐
%   |
% ? | Loads `siunitx`, a unit package.
%   |
%   └───────────────────────────────────────────────────────┘

\usepackage{siunitx}

\ifenglish%

  \sisetup{%
  	locale={US},
  	input-decimal-markers={.},
  	group-separator={\,}
  }

\else

  \sisetup{%
  	locale={DE},
  	input-decimal-markers={\,},
  	group-separator={.}
  }

\fi
