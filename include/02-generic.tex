%   ┌───────────────────────────────────────────────────────┐
%   |
% ? | Loads generic packages needed by almost every.
% ? | document.
%   |
% ! | Must be loaded as the second package.
% ! | Must be loaded for other headers to work.
%   |
%   └───────────────────────────────────────────────────────┘

% ? >> encoding & fonts

\usepackage[T1]{fontenc}
% \usepackage[utf8]{inputenc}

\usepackage{
	textcomp,
	verbatim,
	fancyvrb,
	fontspec,
}

\setmonofont[%
	Contextuals={Alternate},
	Scale=MatchLowercase
]{Fira Code}

% ? >> formatting

\usepackage{%
	float,
	microtype,
	booktabs,
	enumitem,
	emptypage,
	parskip,
	subcaption,
	multicol,
	blindtext,
	titlepic,
	tabularx,
	makecell,
	listliketab,
}

% ? >> links

\usepackage{bookmark}
\usepackage{hyperref}
\hypersetup{hidelinks, colorlinks = false}
\usepackage{url}

% ? >> title page & layout

\usepackage[heightrounded,xetex,pass]{geometry}
\usepackage[usetransparent=false]{svg}

\usepackage[outermarks]{titleps}

\settitlemarks{section,subsection}

\newpagestyle{fancy}{%
	\headrule%
	\sethead{\thesection.~\sectiontitle}{}{\thesubsection.~\subsectiontitle}
	\setfoot{}{\thepage}{}%
}

% ? >> graphics

\PassOptionsToPackage{dvipsnames}{xcolor}
\PassOptionsToPackage{table}{xcolor}

\usepackage{graphicx}
\usepackage{caption}
