%   ┌───────────────────────────────────────────────────────┐
%   │
% ? | Makes sure the acronyms glossary, without
% ? | the default one, is displayed.
%   │
%   │ One might want to add `nogroupskip=true` to the
%   │ package options if `\glsnogroupskiptrue' was not
%   │ used for whatever reason.
%   │
%   └───────────────────────────────────────────────────────┘

\label{appendix}
\cleardoublepage%
\pagenumbering{gobble}
\pagestyle{empty}

\printnoidxglossary[type=\acronymtype]

\ifbibliographyloaded%
	\printbibliography[heading=bibintoc]
\fi
